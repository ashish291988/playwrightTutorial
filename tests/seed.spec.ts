import { test, expect } from '@playwright/test';

test.describe('Test group', () => {
  test('seed', async ({ page }) => {
  // await page.goto('https://selfcare-dev.glasoperator.nl/');
  // await page.locator('[data-test="username"]').click();
  // await page.locator('[data-test="username"]').fill('a.stormvale.fa0f32229b0d43e1b0@stubmail.com');
  // await page.locator('[data-test="password"]').click();
  // await page.locator('[data-test="password"]').fill('test');
  // await page.locator('[data-test="login"]').click();
  // await expect(page.getByRole('link', { name: 'Bekijk abonnement' })).toBeVisible();
  });
});
